# -*- mode: gitconfig; coding: utf-8; indent-tabs-mode: nil; -*-
[user]
        # Only use configuration for user identity, ignore environment variables
        useConfigOnly = true

[include]
        path = personal_dispatch.conf
        path = personal_transcrypt.conf
{%@@ if "khronos3d" in overlays @@%}
        path = khronos3d_dispatch.conf
        path = khronos3d_transcrypt.conf
{%@@ endif @@%}
