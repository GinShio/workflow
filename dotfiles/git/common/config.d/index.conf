# -*- mode: gitconfig; coding: utf-8; indent-tabs-mode: nil; -*-
[index]
        # Use all available CPU cores for index operations
        threads = 0
        # Enable sparse index to only track files in sparse-checkout scope
        sparse = true
        # Use index version 4 for improved performance and features
        version = 4
