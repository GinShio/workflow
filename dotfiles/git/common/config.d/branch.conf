# -*- mode: gitconfig; coding: utf-8; indent-tabs-mode: nil; -*-
[branch]
        # Sort branches by committer date in descending order (newest first)
        # This ensures that when listing branches with git branch, the most recently
        # active branches appear at the top, making them easier to find
        # The '-' prefix indicates reverse chronological order
        sort = -committerdate
