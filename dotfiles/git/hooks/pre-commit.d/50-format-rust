#!/bin/sh
. "$HOOKS_DIR/core/lib.sh"

# Format staged Rust with rustfmt. A missing tool is a silent skip.
command -v rustfmt >/dev/null 2>&1 || exit 0

files=$(staged_lang_files .rs)
oldifs=$IFS
IFS='
'
set -f
rc=0
for f in $files; do
    apply_to_staged "$f" rustfmt --edition 2021 || rc=1
done
IFS=$oldifs
set +f

exit "$rc"
